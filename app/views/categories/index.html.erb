<br>
<br>
<div class="row pad">
  <div class="col-md-6 text-center">
    <h3>Discover the best spots</h3>
  </div>
  <div class="col-md-6 text-center">
    <h3>Select a Cateogry Below</h3>
  </div>
</div>
<div class="row">
  <% @categories.each do |category| %>
  <div class="col-md-3 thumbnail">
    <%= image_tag(category.category_image.large.url) %>
      <div class="caption category-content">
        <%= link_to category.title, category %>
      </div>
  </div>
  <% end %>
</div>

