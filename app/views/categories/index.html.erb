<div class="row pad">
  <div class="col-md-3">
    <h3>Categories</h3>
    <% @categories.each do |category| %>
      <p class="well"><%= link_to category.title, category %></p>
    <% end %>
  </div>
  <h3>Ratings</h3>
  <div class="col-md-9">
  </div>
</div>

